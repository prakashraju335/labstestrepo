name: App C CI

on:
  push:
    branches: [ main ]

jobs:
  call-java-template:
    uses: prakashraju335/firstrepo_apr2026/.github/workflows/java-build.yml@main
    with:
      build-command: './mvnw clean package -pl app-c'
